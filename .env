# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/encrypted-chat

# JWT Secret (Change this to a random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000

# AES Encryption Configuration (Client-side only - no backend keys needed for E2E encryption)
# Note: The actual encryption happens client-side. Backend only stores encrypted data.